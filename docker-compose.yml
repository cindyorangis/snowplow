version: '3.8'

services:
  web:
    build:
      context: . # monorepo root (so all workspace files are available)
      dockerfile: apps/web/Dockerfile # each app has its own Dockerfile
    image: snowplow-web
    container_name: snowplow-web
    ports:
      - '3000:3000'
    env_file:
      - apps/web/.env.production
    restart: unless-stopped
